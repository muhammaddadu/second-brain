# Ruby tooling for macOS code-signing only (fastlane match). The app itself is TypeScript/pnpm —
# this is not needed to build unsigned installers. See docs/guides/building-and-releasing.md.
source "https://rubygems.org"

gem "fastlane"
