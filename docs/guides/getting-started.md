# Getting Started

> **This doc owns:** running the project locally — prerequisites, install, dev loop. **For what the commands do architecturally see** [app-architecture](../architecture/app-architecture.md).

**TODO — nothing to run yet.** The repo currently contains only documentation; there is no `package.json`. This doc gains real content in the same change as epic [E0](../product/epics/E0-vault-core.md) (workspace + commands) and [E1](../product/epics/E1-desktop-shell.md) (launching the app).

## What this doc will own once E0/E1 land

- Prerequisites (Node version, package manager).
- Install and first build.
- The dev loop: run the desktop app against a scratch vault, run tests, lint/typecheck.
- Creating a fixture/scratch vault to develop against (never a personal vault — see [AGENTS.md](../../AGENTS.md) → Git Conventions).

Until then, orient with the [reading paths](../README.md#reading-paths).
